### Task 1: Understanding Branches

1. What is a branch in Git?
 - A branch is a separate workspace where developers work to add, create, or modify code. After verification, it is merged into the main branch if needed, ensuring the main branch of code remains unaffected during development.

2. Why do we use branches instead of committing everything to `main`?
3. What is `HEAD` in Git?
4. What happens to your files when you switch branches?

---
